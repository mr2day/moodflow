import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.serious.moodflow',
  appName: 'Moodflow',
  webDir: 'dist/moodflow/browser'
};

export default config;
